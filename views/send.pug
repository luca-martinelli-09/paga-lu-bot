doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title pagaLU

    script(src="https://telegram.org/js/telegram-web-app.js")
    script(src="https://tg.dev/js/jquery.min.js")
    link(rel="stylesheet" href="css/main.css")
  body(class="flex justify-center")
    #choose(class="screen p-4 active")
      each assets, group in groupedAssets
        div(class="flex flex-col gap-3")
          p(class="hint font-semibold text-xs")= group
          div(class="grid items-start gap-2 grid-cols-3 w-100")
            each asset in assets
              div(class="asset-card" data-id=(asset.ID) data-icon=(asset.Icon) data-name=(asset.Name))
                picture(class="z-10 w-3/4")
                  img(class="w-full" src=("img/assets/" + asset.ID + ".svg") alt=(asset.Name))

                p(class="z-10 font-semibold text-sm")= asset.Name

    #send(class="screen p-0 bg-secondary")
      div(class="flex gap-5 items-center p-4 bg-primary")
        picture
          img#assetPic(class="h-10")

        h2#assetName(class="text-xl font-bold") ...
        input#assetID(type="hidden")

      h1(class="hint font-semibold text-sm px-4") Invia a...

      div(class="flex flex-col gap-2")
        input#receiver(type="text" name="receiver" placeholder="Inserisci l'username del destinatario")
        ul#usersList(class="flex flex-col gap-1")

    #loading(class="fixed inset-0 items-center justify-center")

    #error(class="fixed inset-0 hidden items-center justify-center z-10 bg-slate-900 text-white")
      div(class="flex flex-col gap-8 items-center")
        span(class="text-8xl") 🛑
        span(class="text-2xl") Ops, sembra che non stia usando Telegram

    script(src="js/app.js") 
    script(src="js/send.js") 